node_modules
.next
.vercel
# Local env files (keep the *.example templates tracked).
.env
.env.*
!.env.example
!.env.e2e.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
.swc
.playwright-mcp/
# Playwright artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
deploy-preview.sh
deploy-prod.sh

# local scratch (demo-recorder, frames, mp4s)
claude_workspace/

# Local demo-recording scratch (contains a live auth session in state.json)
.demo/
